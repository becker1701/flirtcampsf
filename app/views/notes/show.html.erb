<%= render 'notes_title' %>

<p id="notice"><%= notice %></p>

<p>
  <strong>Chapter:</strong>
  <%= @note.chapter %>
</p>

<p>
  <strong>Note:</strong><br>
  <%= h(format_newline(@note.note)).html_safe %>
</p>

<%= link_to 'Edit', edit_note_path(@note) %> |
<%= link_to 'Back', notes_path %>
