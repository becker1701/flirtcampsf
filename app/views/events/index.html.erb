<div class="row">
	<div class="col-md-8 col-md-offset-2">
		<% if @events.any? %>

			<%= render @events %>

		<% else %>
			No events exist for this camp.
		<% end %>
		<br>
		<%= link_to "New Event", new_event_path, class: "btn btn-primary" %>
	</div>
</div>

