<% provide(:title, "Sign Up") %>

<h1>Create your Flirt Camp Profile</h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">

		<%= form_for @user do |f| %>

			<%= render 'form', f: f %>

			<%= f.submit 'Create Profile', class: "btn btn-primary" %>

		<% end %>

	</div>
</div>